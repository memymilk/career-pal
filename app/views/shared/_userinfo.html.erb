<div class="d-flex" style="align-items:center">
  <div class="avatar-container">
   <%= image_tag "avatar.jpg", class: "avatar-large"%>

  </div>
    <div class="ms-4">
      <% frame_id = dom_id(current_user, "edit_first_name") %>
      <%= turbo_frame_tag "edit_first_name" do %>
        <%= turbo_frame_tag frame_id do %>
          <h3><%= current_user.first_name %></h3>

        <% end %>
    </div>

    <div class="space-between ms-3 deletion" style="align-self:flex-start"><%= render "shared/edit", path: edit_profile_first_name_path, turbo_frame: frame_id %>
     <% end %>
    </div>

</div>
